{
  "name": "@tennisbot/stereo-replay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build ./src/client.ts --target browser --outdir ./dist --sourcemap=external",
    "serve": "bun ./src/server.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@types/three": "^0.185.0",
    "typescript": "^5.8.0"
  }
}
